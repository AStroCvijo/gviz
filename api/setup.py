from setuptools import setup, find_packages

setup(
    name="gviz-api",
    version="1.0.0",
    description="Graph Visualizer API – shared abstractions for plugins and platform",
    packages=find_packages(),
    python_requires=">=3.9",
    install_requires=[],
)
